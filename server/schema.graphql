type User {
  id: ID
  name: String
  email: String
}
type Post {
  id: ID
  body: String
  user: User
}

type Mutation {
  postCreate(data: PostInput): Post
  postUpdate(id: ID, data: PostInput): Post
  postDelete(id: ID): Post
}

input PostInput {
  body: String
}

type Query {
  posts: [Post]
}
